[artifact]
  slug = "portfolio"
  title = "Portfolio"
  artifact_type = "react-vite"
  preview_path = "/portfolio"
  output_type = "webview"

  [service]
  command = "PORT=3000 BASE_PATH=/portfolio pnpm --filter @workspace/portfolio run dev"
  wait_for_port = 3000
  